[project]
name = "djangobase"
version = "0.1.0"
description = "Django template to create web apps"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dj-database-url>=3.1.2",
    "django>=6.1",
    "django-environ>=0.14.0",
    "django-extensions>=4.1",
]
